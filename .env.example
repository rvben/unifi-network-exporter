# Example environment variables for testing with act
# Copy this file to .env and fill in your actual values
# DO NOT COMMIT the .env file - it contains secrets

# Docker Hub credentials (required for release workflow)
DOCKER_USERNAME=your-docker-username
DOCKER_PASSWORD=your-docker-password

# Crates.io API token (required for release workflow)
CRATES_IO_TOKEN=your-crates-io-token

# GitHub token is automatically provided by GitHub Actions
# For local testing with act, you can generate a personal access token